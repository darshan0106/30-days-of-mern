# User Authentication & Password Hashing (Express + MongoDB)

This project implements a secure user authentication system using Express, MongoDB, and bcryptjs. It includes a User schema with pre-save password hashing, ensuring that no plain-text passwords are stored in the database. The backend provides signup and login APIs with secure password comparison, following industry best practices for building safe and scalable authentication systems in real-world web applications.
