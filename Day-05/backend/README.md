# Joi Validator

This project is a RESTful Book Management API built using Node.js, Express, and MongoDB, following the MVC architecture. It supports full CRUD operations for books with robust input validation using Joi to ensure data integrity before database operations. The application demonstrates clean separation of concerns, scalable backend structure, and proper error handling for real-world API development.
